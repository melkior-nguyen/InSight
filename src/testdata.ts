export type dataType = {
    code: string,
    number: number,
    status: number,
    date: string,
    date_buy: string,
    gate: number,
    checked: boolean,
    type: string,
    price: number,
    disable: boolean
}

